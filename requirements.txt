# --- LLM / agent orchestration ---
langgraph>=0.2.50
langgraph-checkpoint-postgres>=2.0.0
langchain-core>=0.3.0
langchain-openai>=0.2.0          # used to talk to OpenRouter (OpenAI-compatible)

# --- Embeddings: via OpenRouter (qwen3-embedding-8b), no local ML deps ---
# (httpx below is the only client needed)

# --- Database ---
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0
pgvector>=0.3.0

# --- API ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- Utilities ---
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
tiktoken>=0.7.0                  # token counting for chunk sizing
pyyaml>=6.0                      # eval question set
httpx>=0.27.0

# --- Long-term memory (trial: neo4j-labs/agent-memory) ---
neo4j-agent-memory[sentence-transformers]>=0.5.0
bcrypt>=4.1
PyJWT>=2.8

# --- Tests ---
pytest>=8.0
